/*
 * BulmaTable Messages
 *
 * This contains all the text for the BulmaTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BulmaTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BulmaTable component!',
  },
});
